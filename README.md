# targilon5-repo
git@github.com:TomGroman/targilon5-repo.git

on excersize 3 the the reason for the result is that in java' using thread.join throws checked exception, which means that if not handeling the exception by catching it the file will not compile.
